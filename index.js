function Scooter(year, color, model) {
    this.year = year
    this.color = color
    this.model = model
}

let scooter = new Scooter(x, y, z)

function Driver(name, age, experience) {
    this.name = name
    this.age = age
    this.experience = experience
}

let bill = new Driver(a, b, c)

function PickupLocation(address, city) {
    this.address = address
    this.city = city
}

let corner = new PickupLocation(j, k)